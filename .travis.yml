language: python
sudo: false
python:
    - "2.7"
    - "3.4"
    - "3.5"
env:
  matrix:
    - TOXENV=dj18-postgres
    - TOXENV=dj18-mysql
    - TOXENV=dj19-postgres
    - TOXENV=dj19-mysql
    - TOXENV=dj110-postgres
    - TOXENV=dj110-mysql
install:
  - pip install tox
script: tox
